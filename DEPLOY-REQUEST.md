# Заявка Павлу — разворот parsdocs

---

Паш, привет. Хотим запилить **parsdocs** (репо `git.taipit.ru/airesearch/docs-parse`) на `10.10.13.10` — docker compose, без сюрпризов: node + python + postgres + redis, опц. Ollama под локальную LLM. Наружу один порт `doc-service:3000` (UI+API), нужен поддомен `parsdocs.taipit.ru` с TLS через корп. nginx, WebSocket не используем. Корпоративную БД не дёргаем — своя Postgres в compose. Два вопроса до запуска: (1) **куда складывать загруженные документы** — обычный docker volume на хосте ок, или дашь bind-mount под политику бэкапов? (2) **сколько RAM могу взять под пилот** — минимум 16 GB, комфортно 32; GPU был бы плюсом если есть свободная. Секреты передам отдельно когда подведём к старту. Сейчас актуальная история в ветке `feat/bring-phase-3-history`, перед deploy надо смержить в main.

— А. Ляпустин
