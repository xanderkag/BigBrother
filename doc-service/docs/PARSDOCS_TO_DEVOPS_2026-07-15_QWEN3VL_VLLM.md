# ТЗ DevOps: перенести vision-модель `qwen3-vl:32b` с Ollama на vLLM

**Дата:** 2026-07-15
**От:** parsdocs (dev)
**Кому:** DB Support / DevOps (владелец GPU-бокса `10.10.33.10`)
**Приоритет:** высокий — это потолок производительности разбора (см. §1).

---

## 1. Проблема (замерено на проде 2026-07-15)

Разбор упирается в **~30 документов/час**. Диагностика по живым задачам показала:
время ест **не OCR, а LLM-извлечение по картинке**:

| Этап | Время |
|---|---|
| OCR (tesseract) | ~31 с — норма |
| **LLM-извлечение (`qwen3-vl:32b`)** | **434–507 с (7–8 мин/док)** |

Причина: для сканов включён hybrid-routing → извлечение идёт **по изображению**
через тяжёлую 32B vision-модель, а она обслуживается **Ollama** (`10.10.33.10:11434`),
которая **сериализует** запросы (один за другим). 7–8 мин × 4 параллельных слота =
**~30 док/час**. Это ровно тот потолок, на который жалуется бизнес.

Для контраста: **текстовая** модель `qwen3.6` уже переведена на **vLLM**
(`10.10.33.10:8100`) и работает быстро. Осталось сделать то же для vision.

## 2. Решение: вариант B (выделенный инстанс). Вариант A проверен и отклонён.

vLLM даёт continuous batching + параллельный KV-cache → параллельные запросы
обрабатываются вместе, а не по очереди (текстовую модель тот же переход ускорил
~13× под нагрузкой).

### Вариант A — ПРОВЕРЕН, работает технически, но ОТКЛОНЁН (изоляция нагрузки)

Замер на реальном скане (2026-07-15): модель, уже работающая на vLLM
(`qwen36-vllm:8100`, VL-вариант), с флагом «без размышлений»
(`enable_thinking=false` / `reasoning_effort=none`) даёт **чистый OCR за 17с против
38с у Ollama** — то есть технически годна. НО:

- `qwen36-vllm:8100` обслуживает **интерактивные** нагрузки с ризонингом (базы
  знаний, ассистенты с tool-calling — живые пользователи). Свалить туда тяжёлый
  **пакетный** vision parsdocs (4 параллельных потока, крупные картинки) = конкуренция
  за GPU и просадка отзывчивости интерактива. Батч-документы и интерактивный чат на
  одном инстансе держать нельзя.
- (Флаг «без размышлений» — per-request, ризонинг у KB/ассистентов бы не сломал; но
  проблема не в флаге, а в контенции за GPU.)

Поэтому A не берём. Вывод для B: модель **тянет vision на vLLM с thinking-off** —
значит выделенный инстанс точно сработает, нужно только его изолировать.

### Вариант B (выбран) — выделенный vision-инстанс на vLLM, к DevOps

**Поднять `Qwen3-VL 32B` на ОТДЕЛЬНОМ vLLM-инстансе**, изолированном от
интерактивного `qwen36-vllm:8100`. Модель — та же, что сейчас в Ollama
(`qwen3-vl:32b`), движок Ollama → vLLM, отдельный порт (напр. `8101`). Вызовы vision
идут с `enable_thinking=false` (проверено — чисто и быстро). Изоляция гарантирует,
что пакетный разбор документов **не трогает** интерактивные базы знаний и ассистентов.
Детали — §3.

## 3. Технические требования (для варианта B)

- **Модель:** Qwen3-VL 32B Instruct (мультимодальная, vision). Квантизацию (FP8 / AWQ /
  GPTQ / NVFP4 как у `qwen36-vllm`) — на усмотрение DevOps под VRAM-бюджет; главное,
  чтобы это был тот же Qwen3-VL 32B, что сейчас в Ollama (сохранить качество).
- **Движок:** vLLM (та же версия, что для `qwen36-vllm`), OpenAI-совместимый endpoint
  `/v1/chat/completions` с поддержкой **image_url** (data-URL, JPEG) — parsdocs шлёт
  картинку страницы как base64 data-URL.
- **Порт:** отдельный от `qwen36-vllm` (тот на `8100`) — например **`8101`**.
  Итог: `http://10.10.33.10:8101/v1`.
- **VRAM / сосуществование:** бокс Blackwell 96 GB уже держит `qwen36-vllm`
  (~34 GB под vLLM) + Ollama свопает модели (сама `qwen3-vl:32b` в Ollama ~30.8 GB).
  Итог для варианта B: выделенная `qwen3-vl` на vLLM (~30+ GB) в сумме с `qwen36-vllm`
  (~34 GB) = ~64+ GB — по идее влезает в 96 GB, но с KV-cache впритык. Нужно решить,
  делить ли одну карту через `--gpu-memory-utilization` или разнести. **Это ваша зона**
  (мы не управляем VRAM-бюджетом бокса). Если тесно — подскажите, обсудим: возможно,
  после переноса vision на vLLM Ollama-копию `qwen3-vl:32b` можно выгрузить и
  освободить её 30 GB.
- **Мультимодал-лимиты vLLM:** `--limit-mm-per-prompt image=1` (нам достаточно 1
  картинки на запрос), разумный `--max-model-len` (документы у нас усечены до ~12k
  символов текста + 1 картинка; хватит контекста как у текущей Ollama-конфигурации).
- **systemd-сервис** (как `qwen36-vllm`), автозапуск, health-check.

## 4. Контракт вызова (что шлёт parsdocs)

parsdocs ходит на модель **не напрямую**, а через свой `inference-service`
(OpenAI-совместимый прокси). Ему нужен только рабочий `/v1/chat/completions`,
принимающий сообщения вида:

```
messages: [{role:"user", content:[
  {type:"text", text:"<промпт OCR/extract>"},
  {type:"image_url", image_url:{url:"data:image/jpeg;base64,..."}}
]}]
```

Ответ — обычный OpenAI chat completion. Никаких особых заголовков/форматов.
(На стороне parsdocs уже включено сжатие картинки перед отправкой — vLLM получит
уменьшенное изображение, это дополнительно снизит нагрузку.)

## 5. Что сделает parsdocs-сторона ПОСЛЕ (быстро, наша зона)

Как только выделенный vLLM-endpoint (напр. `10.10.33.10:8101/v1`) поднят и отвечает —
с нашей стороны это **минутная правка**: в `provider_settings` строке
`local-qwen3-vl-32b` проставить `extra.backend='openai_compat'` +
`extra.upstream_base_url='http://10.10.33.10:8101/v1'`, и обеспечить на vision-вызовах
флаг **`enable_thinking=false`** (проверено — иначе модель отдаёт reasoning-leak;
с флагом чисто и быстро). TTL-кэш подхватит за минуты (или мелкий рестарт api/worker).
Редеплоя кода не требуется.

**Дайте знать endpoint (IP:порт) — и мы переключаем.**

## 6. Приёмка

- Одиночный vision-вызов (OCR страницы / extract по картинке) возвращается за
  **секунды–десятки секунд**, не минуты.
- Параллельные запросы обрабатываются батчами (не сериализуются как в Ollama).
- Разбор скана «под ключ» уходит с ~8 мин к целевым ~1–2 мин; пропускная
  способность — с ~30/час в разы выше.
- Точность извлечения — не хуже текущей (модель та же).

## 7. Откат

Ollama-обслуживание `qwen3-vl:32b` (`10.10.33.10:11434`) **оставить как есть** —
это наш fallback. Если с vLLM что-то не так, parsdocs-сторона возвращает
`upstream_base_url` на Ollama одной правкой строки. Ничего не ломаем необратимо.

---

**Резюме:** вариант A (reuse `qwen36-vllm:8100`) проверен — технически работает
(чистый OCR 17с vs 38с Ollama), но **отклонён**: этот инстанс обслуживает
интерактивные ризонинг-нагрузки (базы знаний, ассистенты с tool-calling), и
пакетный vision parsdocs нельзя ставить с ними на один GPU. Идём **вариантом B**:
DevOps поднимает **выделенный, изолированный** инстанс `Qwen3-VL 32B` на vLLM
(порт напр. `8101`, OpenAI-совместимый, multimodal, `enable_thinking=false` на
vision), VRAM-бюджет — за DevOps. Главный тормоз (7-8 мин image-extract на Ollama)
снимается; переключение provider-строки — минутная правка у нас, откат на Ollama
сохранён. **Отпишите: влезает ли выделенный инстанс рядом с `qwen36-vllm` и на какой
порт — и мы переключаемся.**
