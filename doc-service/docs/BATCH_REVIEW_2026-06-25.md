# ТЗ по итогам боевого батча TAIPIT (заказы) — 2026-06-25

> Сформировано по мониторингу первого боевого прогона реальных доков по заказам.
> Анализ на агрегатах (типы / статусы / confidence / заполненность полей) —
> содержимое документов не читалось.

## Прогон
- **12 доков, все `done`**, 0 падений, очередь пуста.
- Confidence здоровый: 7× ≥90%, 5× 70–90%, ниже 70% нет.
- Извлечение — локальная **phi4** (10.10.33.10), облачные провайдеры выключены
  (корп-данные в облако не уходят), gateway-каналы спят (503).
- Состав: invoice ×4, bill_of_lading ×2, commercial_invoice, proforma_invoice,
  packing_list, power_of_attorney, wire_transfer_application, «не определён» ×1.

## ✅ Исправлено и задеплоено в ходе мониторинга
1. **Классификатор: счёт «С Ч Е Т» вразрядку → invoice** (раньше уезжал в
   `wire_transfer_application` → терял позиции). Миграция `20260625000001`.
   Проверено вживую: счёт без хинта → invoice + позиция.
2. **Форма Extracted data — информативнее**: ~120 русских подписей + гуманизатор
   ключей + таблица позиций даже без колонок в схеме + бейдж типа (`display_name`
   + описание). Commit `2a00692`.
3. **B/L регрессия (критично для SLAI)**: `bill_of_lading` извлекал ТОЛЬКО `items`
   (минимальный DB-skeleton из merge-миграции `20260607999999` перекрывал полную
   BL_SCHEMA из кода). Resolver дополнен fallback'ом на EXTENDED_SCHEMAS +
   миграция `20260625000002` (BL llm_schema=NULL). Проверено: B/L теперь даёт
   number / shipper / consignee / containers / порты / vessel / вес + `bl_number`
   в match-signals.

## Ремедиация уже-прогнанных тест-доков
- `be669758` (счёт со скриншота): был «платёжка» → переклассифицирован → **invoice** + позиция.
- `4e9e9dd2`: оказался **настоящей доверенностью** — классификация верна.
- 2× `bill_of_lading`: перепрогнаны → полные поля.
- `aa551d6e` (договор): пока 0 полей — ждёт пункт P1-1 ниже.

## Открытые пункты (ТЗ)

### P1 — влияет на качество прямо сейчас
1. **Договоры не распознаются.** Тип `contract` в каталоге есть (17 ключей
   классификации), но реальный договор ушёл в «не определён» → 0 извлечённых
   полей. Нужно: (а) расширить `classification_keywords` у `contract` под реальные
   формулировки заголовков (нужны 1–2 обезличенных образца), (б) убедиться, что у
   `contract` задана `llm_schema` (иначе даже после верной классификации полей не
   будет). Договоры — частый тип в заказах, приоритет высокий.
2. ✅ **ВЫПОЛНЕНО** — **B/L: контейнеры не доходили до match-signals.** Причина:
   модель приклеивала к номеру тип/размер («MRKU1234567 40HC», 13 символов), а
   `collectContainers` требовал совпадения ВСЕЙ строки с маской ISO-6346 → отсекал.
   Фикс (commit `2c51234`): извлекаем ISO-6346 как ПОДСТРОКУ `[A-Z]{4}\s?\d{7}`.
   Проверено: оба B/L → `_match_signals.containers=["FITU5526116"]`.

### P2 — мониторить на большом объёме
3. **Низкий confidence** у `commercial_invoice` (0.71), `packing_list` (0.74),
   `bill_of_lading` (0.83). На 12 доках не криминал, но на большом батче следить:
   при мисклассах/недоизвлечении — тюнить ключи/схемы по образцам.

### P3 — операционное удобство
4. ✅ **ВЫПОЛНЕНО** — **«Определить тип заново».** Бэкенд: `POST /jobs/:id/reprocess?reclassify=true`
   игнорирует сохранённый тип/хинт → классификатор с нуля (commit `a4b7af6`). UI: кнопка
   «Определить тип заново» на JobDetail рядом с «Перепрогнать» + ConfirmDialog (commit `c226460`).

## Статус (обновлено 2026-06-25)
- ✅ Сделано в ходе мониторинга: классификатор счёта-вразрядку, информативная Форма,
  B/L-регрессия (только-items), B/L-контейнеры (ISO-подстрока), кнопка «Определить тип заново».
- 🟡 **Осталось — P1-1 договоры (нужен образец).** Тип `contract` есть, схема есть, но
  реальный договор не классифицировался ни одним из 17 структурных ключей (только
  инфлектированный стем «договорА»). Расширять вслепую НЕЛЬЗЯ: счета/акты ссылаются на
  «Договор №X» в теле → улетят в contract. Нужен 1–2 обезличенных образца договора, чтобы
  добавить ключ под реальный заголовок, не ломая счета.
- 🟡 P2 — мониторить confidence commercial_invoice/packing_list на большом объёме.

## Вывод
Конвейер на реальных данных держится: классификация в основном верная, извлечение
по invoice/BL полное, контейнеры доходят до SLAI, падений нет, корп-данные локальны.
Единственный незакрытый пункт — распознавание договоров (ждёт образец).
