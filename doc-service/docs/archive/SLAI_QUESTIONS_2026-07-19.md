# Вопросы parsdocs от SLAI: сверка типов документов (2026-07-19)

Свели живой поток `document_type` за последние дни с вашим каталогом 51 типа
(нота `SLAI_NEWTYPES_2026-07-16.md`). В целом всё сходится, но всплыли 3 типа,
которые вы **шлёте вживую сегодня**, а в каталоге 51 их **нет**. Хотим свериться,
чтобы правильно их разложить у себя.

## 1. Три типа вне каталога — они канонические?

| slug | сколько за ~2 недели | в каталоге 51 | что сделали у себя |
|---|---|---|---|
| `forwarding_order` | ~97 | ❌ нет | ярлык «Поручение экспедитору», маршрут = уровень заказа |
| `services_act` | ~69 | ❌ нет | ярлык «Акт оказанных услуг», семейство как у `AKT` |
| `tax_invoice` | ~47 | ❌ нет (есть `factInvoice`) | уже был у нас, семейство счёта |

Вопрос: это **стабильные канонические slug'и** (не переименуете) — просто хвост,
который не попал в список 51? Если да — добавьте их в каталог, чтобы список был
полный. Если это временные/бета-имена — скажите, на что заменятся.

## 2. Связь с уже существующими типами

- `services_act` vs `AKT` (в каталоге есть общий `AKT`): это **разные** типы
  (акт услуг ≠ общий акт) или `services_act` — уточнение `AKT`? Мы развели их как
  разные. Ок?
- `tax_invoice` vs `factInvoice` (в каталоге есть `factInvoice` = счёт-фактура):
  это одно и то же (налоговый счёт-фактура) или разное? Сейчас мапим оба в
  семейство счёта, но хотим понять, слать ли их как один тип.

## 3. Каталог актуален?

Список 51 датирован 2026-07-16. Были ли **после** этого ещё переименования/новые
типы, которых мы могли не заметить? Если да — короткий дифф (что→что), и мы
подтянем словарь. Наша сторона к незнакомым типам устойчива (не падаем, карточка
«как есть»), но без сверки юзер видит сырой slug вместо русского названия.

## Что уже сделали у себя (не ждём вас)

Добавили ярлыки и маршрутизацию для `forwarding_order` и `services_act` — влито
и на стенде. Нужен только ваш ответ по пунктам 1–3, чтобы зафиксировать канон.

---

## 4. Просьба к Vanga: код ТНВЭД отдельным полем

Отдельный запрос по извлечению (Vanga). Собираем справочник **ТНВЭД** и он же
нужен для графы 33 ЭТД. Проблема: код ТНВЭД сейчас почти не приезжает **полем** —
`hs_code` на верхнем уровне встретился в единицах документов, а в основном код
**зашит в текст** названий позиций/услуг («…контейнер ГНГ 94032080, ЕТСНГ 41302»).
Из текста выковыривать ненадёжно.

Просьба: извлекать **код ТНВЭД структурированным полем** —
- в позиции товара: `items[].hs_code` (и, где есть, `items[].hs_description`);
- на уровне документа/декларации: `hs_codes[]` или плоский `hs_code`, если код один.

Формат — как договоримся (6 или 10 знаков, только цифры). Это разблокирует у нас
сбор справочника кодов и автозаполнение графы 33 ЭТД (по аналогии с тем, как
почистили ИНН). Скажите, реалистично ли и в какие сроки.

## 5. Office-файлы (.xls/.doc/.docx) — подтвердите, что конвертация у вас

Проверили у себя: почти все office-файлы (.xlsx/.docx/.xls) в вашей очереди со
статусом `done`, реальных сбоев конвертации — **2 за всё время** (остальные
«failed» = наши же stale-пометки от чистки зомби-очереди 14.07). Похоже, вы уже
конвертите office сами (LibreOffice/иначе) — старая проблема «.xls вешал воркер
на 30 мин» больше не воспроизводится.

Подтвердите: **конвертация office → текст/PDF полностью на вашей стороне**? Если
да — снимаем у себя задачу на предобработку (не будем дублировать LibreOffice
перед отправкой). Если есть типы, что всё же падают — назовите, добавим конвертацию
у себя на них точечно.

— SLAI (Тайпит)
