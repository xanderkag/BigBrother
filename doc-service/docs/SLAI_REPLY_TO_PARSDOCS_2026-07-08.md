# SLAI → parsdocs: ответ на сверку от 07.07

**Дата:** 2026-07-08
**От:** SLAI
**Кому:** parsdocs (Vanga / Big Brother)

---

Привет! Прошли вашу сверку, отвечаю по пунктам.

Сначала главное: почти всё, что вы просите у нас по контракту, на приёме уже
готово и в проде — `unknown` принимаем, `file_sha256` читаем и дедупим по нему,
версии по `document_stage` выбираем. Спасибо, что вывели хэш и стадию наверх,
легли ровно.

**Контракт**

- **Q19 (`unknown`)** — принимаем, не роняем, уводим в ручную разборку. Закрыто.
  Просьба: снимите у себя в очереди статус OPEN — по коду и вашему note от 01.07
  он давно закрыт.
- **Q16 (containers у инвойса)** — видим, читаем как у B/L. Спасибо.
- **Q17 (`schema_version`)** — начинаем читать. Как договаривались: MINOR/PATCH
  логируем, MAJOR гейтим. На матчере предупреждение при несовпадении уже есть,
  добавим лог и на самом приёме.
- **Schema 1.2** — принимаем, всё аддитивно, ничего не ломает. Из новых BL-полей
  подключим `master_bl_number` и `bl_type` (нужны, чтобы сшивать Master и House
  в одну семью). `release_type` и `number_of_original_bls` пока просто принимаем.

**Ваши вопросы (§5)**

- **A (WW-23 / переход на боевой контур)** — открытых багов за нами не держим.
  За cutover с Asha на ваш прод, предлагаем сразу после демо. Точную дату назовём,
  как зафиксируем показ.
- **B (30 пустых инвойсов)** — переотдайте вебхуком по списку `job_id`. Приём у нас
  идемпотентный по `job_id`, дублей не создаст и ручные правки не затрёт — нам так
  проще, чем re-pull.
- **C (`hs_code` как кросс-ключ)** — полезно для ВЭД-цепочки (инвойс↔пакинг↔ГТД),
  но не в этот заход. Предлагаем отдельной итерацией после демо, тогда и bump 1.3.
- **D (PDF Росавтодора)** и **E (waybill + ВЭД-инвойс)** — пока дать не можем,
  доступа к боевым образцам сейчас нет. Пришлём, как появятся; до тех пор эти два
  типа доводите на своих данных.
- **F (secret для `slai-negabarit`)** — давайте заведём отдельный, так чище.
  Передадим безопасным каналом, не в переписке.
- **G (стадии документов)** — в основном `original` (первичный документооборот).
  Но `copy` реально бывает: по коносаментам приходило до 7 копий на заказ. `draft`
  редко. Расширять enum не нужно, а `copy` в потоке учитывайте.

**Флаги (§6) — порядок включения**

Внутренние (`HYBRID_ROUTING`, `FILE_URL_INGEST`) — включайте когда удобно, нас не
блокирует. Клиентские (`chat`, `embeddings`, `dadata`) — одним пакетом после демо,
синхронно, с пингом за сутки-двое. `BYO_LLM` пока не трогаем.

Если по чему-то нужны детали — пишите.
