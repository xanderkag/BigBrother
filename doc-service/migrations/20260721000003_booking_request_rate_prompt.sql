-- Up Migration
--
-- Аудит пустых доков 2026-07-21: единственный подтверждённый недобор
-- ЭКСТРАКТОРА во всей выборке — booking_request не извлекает блок ставки
-- (rate), хотя поле есть в схеме и ставка с валютой явно присутствует в
-- тексте заявок (4/24 дока аудита, 2 уникальных + дубликаты). Модель
-- проскакивает блок «ставка/стоимость перевозки» в табличной шапке .doc-форм.
-- Append промпт-инструкции (по образцу 20260629000001). Деплой: migrate +
-- restart api/worker (сброс кэша резолвера).

BEGIN;

UPDATE document_types
SET llm_prompt = COALESCE(llm_prompt, '') || E'\n\nВАЖНО про ставку: блок «Ставка», «Стоимость перевозки», «Фрахт», «Rate», «Цена услуги» (часто в табличной шапке заявки) — извлекай в rate: числовое значение + валюта + за что (за контейнер/кг/рейс), как в документе. Если ставка «по согласованию»/«договорная» — пиши это строкой. Дополнительные услуги с ценами — отдельно, не смешивай со ставкой перевозки.',
    updated_at = now()
WHERE slug = 'booking_request';

COMMIT;
