/**
 * Стабильные UUID для дефолтных tenant-сущностей.
 *
 * Эти id посеяны в миграции 008. Код ссылается на них напрямую без
 * лишних JOIN'ов — нужно «дефолтный проект для legacy-job'а» — берём
 * `DEFAULT_PROJECT_ID`. Менять значения нельзя, иначе backfill в БД
 * перестанет совпадать.
 */
export const SYSTEM_ORG_ID = '00000000-0000-0000-0000-00000000a001';
export const DEFAULT_PROJECT_ID = '00000000-0000-0000-0000-00000000a002';
export const SYSTEM_USER_ID = '00000000-0000-0000-0000-00000000a003';
