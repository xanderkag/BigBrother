samples/
*-report.json
golden-set*.json
# run outputs embed expected values + extracted PII — keep local
qwenvl-real-*.json
jobmap.json
_extract.txt
# cached OCR/extracted text of real documents — confidential, keep local
_textcache/
